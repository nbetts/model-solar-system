import { Stars } from "@react-three/drei";

const SpaceBackground = () => {
  return <Stars radius={20000000000} depth={100000000000} count={20000} factor={1700000000} />;
};

export default SpaceBackground;
